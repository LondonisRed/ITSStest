package test;

/*import app.aims_store.AimsApplication;
import controller.RushShippingFormController;
import javafx.application.Application;
import javafx.fxml.FXMLLoader;
import javafx.scene.Scene;
import javafx.stage.Stage;

import java.io.IOException;

public class RushShippingFormTest extends Application {

    private RushShippingFormController controller;

    @Override
    public void start(Stage stage) throws Exception {
        FXMLLoader loader = new FXMLLoader(AimsApplication.class.getResource("RushShipping.fxml"));
        stage.setScene(new Scene(loader.load(), 1920, 1080));
        controller = loader.getController();
        stage.show();
    }

    public static void main(String[] args) {
        launch();
    }
}*/
